t=int(input())
for k in range(t):
    n,k=map(int,input().split())
    l=list(map(int,input().split()))
    for j in range(k):
        m=l[n-1]
        for i in range(len(l)-1,-1,-1):
            if i!=0:
                l[i]=l[i-1]
        l[0]=m
    print(*l)
        
        
            
        
