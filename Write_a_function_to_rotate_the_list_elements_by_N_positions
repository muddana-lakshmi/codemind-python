n=int(input())
l=list(map(int,input().split()))
k=int(input())
for i in range(k):
    m=l[n-1]
    for j in range(len(l)-1,-1,-1):
        if j!=0:
            l[j]=l[j-1]
    l[0]=m
print(*l)
        
